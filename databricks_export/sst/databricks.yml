# This is a Databricks asset bundle definition for sst.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: sst
  uuid: 570652bb-94fd-46e1-95f9-d348d782fe34

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://4437281602191762.2.gcp.databricks.com

  prod:
    mode: production
    workspace:
      host: https://4437281602191762.2.gcp.databricks.com
      # We explicitly specify /Workspace/Users/zak.miller@datakind.org to make sure we only have a single copy.
      root_path: /Workspace/Users/zak.miller@datakind.org/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: zak.miller@datakind.org
        level: CAN_MANAGE
    run_as:
      user_name: zak.miller@datakind.org
